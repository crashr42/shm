<% content_for :local_menu do %>
  <li><%= link_to 'some_link', '.link1' %></li>
<% end  %>

<div class="page-header">
<h4><small>Вы просматриваете прием</small> № <strong> <%= @a.id %> </strong><br/>
 <small>Прием создан </small> <%= get_appointment_organizer(@a)  %>
  <small>от  </small> <%= @a.created_at.strftime("%v") %> <br>
  <small>Пациент </small> <%= link_to @a.patient_user.first.shortname, "/user/profile/#{@a.patient_user.first.id}"  %>
</h4>
</div>

<h5>
 <%= @a.summary  %><br/>
 <small><%= @a.description  %></small>
</h5>
</table>

<% if @a.is_processing? %>
  <%= render :partial => 'medicaments_assigning' %>
<% end %>

<%= render :partial => 'available_actions' %>