<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Shm" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <link href="images/apple-touch-icon.png" rel="apple-touch-icon">
    <link href="images/apple-touch-icon-72x72.png" rel="apple-touch-icon" sizes="72x72">
    <link href="images/apple-touch-icon-114x114.png" rel="apple-touch-icon" sizes="114x114">
  </head>
  <body data-controller="<%= params[:controller] %>" data-action="<%= params[:action] %>">

    <div class="navbar navbar-fixed-top">
      <div class="navbar-inner">
        <div class="container">
          <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </a>
          <%= link_to_active 'Shm', {:controller => :'/index', :action => :index}, :class => 'brand' %>
          <div class="container nav-collapse">
            <ul class="nav">
              <%= link_to_active_wrapper 'Dashboard', :li, :controller => :index, :action => :index %>
              <%= link_to_active_wrapper 'Users', :li, :controller => :user, :action => :index  %>
              <li class="dropdown <% if controller.controller_name == 'parameter' %> active <% end %>">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Parameters <b class="caret"></b></a>
                <ul class="dropdown-menu">
                  <%= link_to_active_wrapper 'List', :li, :controller => :parameter, :action => :index %>
                  <%= link_to_active_wrapper 'Add', :li, :controller => :parameter, :action => :new %>
                </ul>
              </li>
              <%= link_to_active_wrapper 'Bids', :li, :controller => :'cabinet/manager/bid', :action => :index %>
              <%= link_to_active_wrapper 'New event', :li, :controller => :event, :action => :new %>
              <%= link_to_active_wrapper 'New rule', :li, :controller => :recurrence, :action => :new %>
            </ul>
          </div><!--/.nav-collapse -->
        </div>
      </div>
    </div>

    <div class="container">
      <div class="content">
        <div class="row-fluid">
          <%= yield %>
        </div><!--/row-->
      </div><!--/content-->

      <footer>
        <p>&copy; Company 2012</p>
      </footer>

    </div> <!-- /container -->

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->
    <%= include_gon %>
    <%= javascript_include_tag "application" %>

  </body>
</html>
