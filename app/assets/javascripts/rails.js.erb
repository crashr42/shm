define([], function(){
    return {
        // Текущая среда разработки
        env: '<%= Rails.env %>',
        // Текущий контроллер
        controller: document.body.getAttribute('data-controller') || undefined,
        // Текущее действие
        action: document.body.getAttribute('data-action') || undefined
    };
});